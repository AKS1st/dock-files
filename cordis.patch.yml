# dock-files bundle patch: mounts the file-explorer feature plugin row
# (client half registers its panel/activity item against the workbench base).
- insert:
    - id: dock-files
      name: 'dock-files'
